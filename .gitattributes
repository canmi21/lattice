# Machine output, marked so the language statistics describe this repository rather than the
# subsetter and the image pipeline. See spec/architecture/workspace.md, "Machine output is
# marked, so the language statistics describe the repository".
libs/fonts/src/*.css linguist-generated=true
data/metadata.json linguist-generated=true
